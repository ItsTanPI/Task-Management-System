version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./TaskSystem.db:/app/TaskSystem.db
    restart: unless-stopped
